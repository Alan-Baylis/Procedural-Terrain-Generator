using UnityEngine;
using System.Collections;

public class NextNextWizard : MonoBehaviour {

	int x = 0;

	/*

	void Update () {
		
		if (Input.GetMouseButtonDown (0)) {
			switch(x){
			case 0: createTerrain(); break;//pravljenje terena
			case 1: createTexture(); break;//lepljenje tekstura
			case 2: createBiljek(); break;//biljke
			case 3: createCities(htmap, m_terrain, m_heightMapSize ,object1, object2, object3); break;//gradovi
			}
			++x;
		}
	}
	
	void OnGUI(){
		GUI.Box (new Rect (Screen.width/2-200, 20, 400, 30), "KLIKNUTI ZA SLEDECU FAZU");
	}

	void createTerrain(){
	}

	void createTexture(){
	}

	void createBiljek(){

	}

	void createCities(float[,] htmap,Terrain t, int tSize, GameObject object1,GameObject object2,GameObject object3){
		Field.start (htmap, t, tSize ,object1, object2, object3);
	}*/

}
